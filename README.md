# ai-readiness-assessment

Part of the [GENIA Americas AI Toolkit](https://github.com/GENIA-Americas) — repo #1, the
entry point. Backend-only API (no frontend) for scoring an organization's readiness to adopt AI.

## Stack

Versions below were verified by actually installing them, not assumed from memory:

- fastapi 0.139.2
- pydantic 2.13.4
- SQLAlchemy 2.0.51
- uvicorn 0.51.0
- pytest 9.1.1 / httpx 0.28.1, httpx2 2.7.0 (dev/test only)

These will drift over time. Run `pip list --outdated` yourself before pinning newer versions.

## Setup

```bash
python3 -m venv .venv
source .venv/bin/activate   # on Windows: .venv\Scripts\activate
pip install -r requirements.txt
cp .env.example .env
```

## Run

```bash
uvicorn app.main:app --reload
```

Interactive API docs (auto-generated by FastAPI): http://127.0.0.1:8000/docs

## Test

```bash
pytest tests/ -v
```

10/10 tests pass as of last verified run.

## Auth

All endpoints require an `X-API-Key` header. Keys are configured via the `API_KEYS`
env var as `key1:org_one,key2:org_two` — each key authenticates its caller as exactly
one org, and reads/writes are scoped to that org.

## API

- `POST /assessments` — submit a questionnaire, get back scored categories, overall score
  (0-100), and a readiness tier (`Not Yet Ready` / `Foundational` / `Emerging` / `AI-Ready`).
  Org name is derived from your API key, not sent in the request body.
- `GET /assessments/{id}` — retrieve one assessment belonging to your org.
- `GET /assessments` — list assessments belonging to your org, most recent first.
- `GET /health` — liveness check.

## Scoring rubric

Five weighted categories, each averaged from two 0-5 questions, weights sum to 1.0:

| Category | Weight |
|---|---|
| Data readiness | 0.25 |
| Talent | 0.20 |
| Governance | 0.20 |
| Infrastructure | 0.20 |
| Leadership | 0.15 |

See `app/scoring.py` for the exact calculation and tier thresholds.

## Next in the toolkit

Repo #2, `ai-maturity-evaluation`, reuses this scoring pattern for longitudinal benchmarking.
